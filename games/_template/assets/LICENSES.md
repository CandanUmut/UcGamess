# Asset licenses — game-template

Every file in this directory must have an entry below. `pnpm check-licenses`
fails the build otherwise, and CI runs it on every PR.

This is not bureaucracy. Two concrete risks it manages:

- **Font and asset embedding rights.** A web game ships its fonts and art to the
  player's browser. "Free for personal use" is not enough — we need commercial
  use *and* embedding rights. Getting this wrong is a takedown, not a warning.
- **AI provenance.** Fully AI-generated assets have no copyright protection in
  the US, meaning competitors can legally copy them. Poki's AI policy also
  requires that AI be used to enhance rather than replace craft. If an asset was
  AI-assisted we record the tool, the prompt and what a human changed
  afterwards, so we can answer that question years later.

See `docs/assets.md` for approved sources and the full policy.

## Required fields

| Field | Notes |
| --- | --- |
| `File` | Path relative to this directory |
| `Source` | Where it came from (site, tool, or "original work") |
| `Author` | Person or studio credited |
| `License` | CC0, CC-BY 4.0, OFL, a purchased license, etc. |
| `URL` | Link to the asset or its license terms |
| `AI` | `No`, or the tool + prompt + human editing done |

---

## Assets

### logo.png

- **File:** `logo.png`
- **Source:** Original work — generated procedurally with a Node script during
  repo setup (see the commit that added this file).
- **Author:** UC Games
- **License:** CC0-1.0 (we own it; released into the public domain)
- **URL:** n/a — original work, not published elsewhere
- **AI:** No. Drawn programmatically from explicit pixel coordinates, not
  generated by a model.
- **Notes:** Deliberately tiny (477 bytes). The template ships almost no assets
  so a new game starts with the entire size budget available; everything else
  on screen is drawn with Phaser primitives.

---

## Template for new entries

Copy this block when you add an asset:

```markdown
### <filename>

- **File:** `<path/relative/to/assets>`
- **Source:** <e.g. Kenney.nl "Platformer Pack Redux">
- **Author:** <e.g. Kenney>
- **License:** <e.g. CC0-1.0>
- **URL:** <link>
- **AI:** No
- **Notes:** <anything relevant — modifications made, restrictions, attribution
  text that must appear in-game>
```

For an AI-assisted asset, the `AI` field must read like:

```markdown
- **AI:** Adobe Firefly (Image 3). Prompt: "flat vector shield icon, mint green,
  no text". Regenerated 4 times, then recoloured and the outline redrawn by hand
  in Affinity Designer. No copyrighted style or character was referenced.
```
