/**
 * The game's public name — the menu, the rotate prompt, the tab title, and
 * every store cover `tools/store.mjs` renders. Renaming the game is this line
 * plus `<title>` in index.html and the text in `store/listing.md`.
 *
 * "Beeline" is taken by several games on itch.io; pick a distinct name before
 * a public release.
 */
export const GAME_TITLE = 'Beeline';
